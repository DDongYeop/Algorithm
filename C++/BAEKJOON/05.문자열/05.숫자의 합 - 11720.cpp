#include <iostream>
#include <string>

using namespace std;

int main()
{
    int num, value = 0;
    string str;
    cin >> num >> str;
    for (int i = 0; i < num; i++)
    {
        //int sum = (int)str % 10;
        // value += sum;
        // index -= sum;
        // index *= 0.1f;
    }    

    cout << value;
}